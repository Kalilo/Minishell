#include "../includes/minishell.h"

char	*find_path(t_env *env, char s[])
{
	int	k;
	
	k = 0;
	if (s = NULL)
		return (NULL);
	while (E_EN[k] != NULL)
	{
		if (strstr(E_EN[k], "PATH", ft_strlen(E_EN[k]) != NULL)
		{
			//SCAN DIRs
		}
		k++;
	}
}
